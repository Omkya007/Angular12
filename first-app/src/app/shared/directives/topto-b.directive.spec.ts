import { ToptoBDirective } from './topto-b.directive';

describe('ToptoBDirective', () => {
  it('should create an instance', () => {
    const directive = new ToptoBDirective();
    expect(directive).toBeTruthy();
  });
});
