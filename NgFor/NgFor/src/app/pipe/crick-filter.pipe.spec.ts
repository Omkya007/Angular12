import { CrickFilterPipe } from './crick-filter.pipe';

describe('CrickFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CrickFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
