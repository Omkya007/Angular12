import { PlayFilterPipe } from './play-filter.pipe';

describe('PlayFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PlayFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
